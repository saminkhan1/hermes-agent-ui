# agent-UI Human E2E Fixtures

The runner creates per-run fixture files under `.agent-ui-eval/runs/<runId>/workspace/`.
This directory exists to keep fixture ownership explicit and to hold stable shared fixtures if a future scenario needs them.
